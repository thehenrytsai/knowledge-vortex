# knowledge-vortex

## Publishing to GitHub Pages

```sh
npm install
```

Build the website:

```sh
npm run build
```

Deploy to `gh-pages` branch which the website will be served from:

```sh
npm run deploy
```
